# =============================================================================
# Common requirements shared across all backend services
# =============================================================================
# Each service's own requirements.txt should reference service-specific deps
# on top of this file.  Install with:
#   pip install -r ../requirements.txt -r requirements.txt
# =============================================================================

# --- Web framework & server --------------------------------------------------
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9        # form / file-upload support in FastAPI

# --- Configuration & environment ---------------------------------------------
pydantic-settings>=2.2.1
python-dotenv>=1.0.1

# --- Auth / JWT (needed by any service that validates tokens) ----------------
pyjwt>=2.8.0

# --- Caching / rate-limiting / session store ---------------------------------
redis>=5.0.3

# --- Inter-service HTTP calls ------------------------------------------------
httpx>=0.27.0

# --- Utilities ---------------------------------------------------------------
python-dateutil>=2.9.0         # date/time parsing helpers
